import { Text } from '@/components/ui/text';
import { View } from "react-native";

export default function EditProfile() {

  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text variant={'h1'}>Modifica Profilo</Text>
    </View>
  );
}
